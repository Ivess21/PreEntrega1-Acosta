import {Box,Center, ChakraProvider} from '@chakra-ui/react';
const ItemListContainer = ({greeting}) => {
    return <Box bgColor={'#EDEDED'}color={'#000'}>{greeting}

    </Box>
}
export default ItemListContainer